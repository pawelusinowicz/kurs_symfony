{% extends "@FOSUser/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <h3>Witaj!</h3>
    <p>Jeśli chcesz dodać aukcję, wejdź pod ten link <a href="{{ url("my_auction_add") }}">Link</a></p>
    <p>Jeśli chcesz zobaczyć swoje aukcje, wejdź pod ten link <a href="{{ url("my_auction_index") }}">Link</a></p>
    {% if targetUrl %}
    <p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>
    {% endif %}
{% endblock fos_user_content %}
